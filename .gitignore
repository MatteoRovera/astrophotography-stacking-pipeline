.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.claude/

# os and editor junk
.DS_Store
Thumbs.db

# raw astro data and full-resolution outputs are large personal binaries,
# kept local instead of in the repo. cocoon_result.tiff also exceeds
# github's 100mb per-file limit. the downscaled preview png is tracked.
COCOON NEBULA*/
COCOON NEBULA*.zip
cocoon_result.tiff
*.CR2
*.fits
*.fit
